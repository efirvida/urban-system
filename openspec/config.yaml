# openspec/config.yaml
# SDD bootstrap for project: vrp
# Generated by sdd-init on 2026-06-16 (greenfield, no files detected).

schema: spec-driven

context: |
  Project: vrp (Vehicle Routing Problem solver)
  Stack: Next.js (planned) — TypeScript, App Router. Not yet scaffolded.
  Architecture: To be defined in the first change's design.md.
  Testing: none detected (no package.json, no test runner). strict_tdd disabled.
  Style/conventions: not yet established.
  Git: not initialized — recommend `git init` as part of the bootstrap change.
  Language (artifacts): English (default).

# Project-wide SDD conventions. Per-phase knobs are below.
strict_tdd: false  # disabled: no test runner present; enable when one is added.

rules:
  proposal:
    - Include rollback plan for risky changes
    - Note any new tooling or dependencies the change introduces
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Delta specs MUST be self-contained (full updated requirement block)
  design:
    - Document architecture decisions with rationale (ADR-style)
    - Include sequence diagrams for complex flows
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Pair every implementation task with a test task when strict_tdd is on
  apply:
    tdd: false
    test_command: ""           # populate when a test runner is added
    follow_existing_patterns: true
  verify:
    test_command: ""           # populate when a test runner is added
    build_command: ""          # populate when a build is configured
    coverage_threshold: 0      # populate when coverage tool is added
  archive:
    - Warn before merging destructive deltas

# Persisted testing capability snapshot.
# Source of truth: same field under `testing:` plus the .atl/ cache when present.
testing:
  strict_tdd: false
  detected_at: "2026-06-16"
  runner:
    command: ""
    framework: ""
  layers:
    unit:        { available: false, tool: "" }
    integration: { available: false, tool: "" }
    e2e:         { available: false, tool: "" }
  coverage:
    available: false
    command: ""
  quality:
    linter:       { available: false, command: "" }
    type_checker: { available: false, command: "" }
    formatter:    { available: false, command: "" }
  notes: |
    Greenfield project — no package.json, no test runner, no tooling detected.
    The first change ("bootstrap-nextjs") should add: Next.js + TypeScript,
    a test runner (vitest recommended for Next.js), ESLint, Prettier, and
    populate the commands above.
